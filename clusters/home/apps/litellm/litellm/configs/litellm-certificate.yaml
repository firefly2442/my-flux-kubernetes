apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: litellm-cert
  namespace: litellm
spec:
  secretName: litellm-certificate-secret
  dnsNames:
    - "litellm.homelab.rivetcode.com"
  issuerRef:
    name: cloudflare-clusterissuer
    kind: ClusterIssuer
