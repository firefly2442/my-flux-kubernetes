apiVersion: v1
kind: Service
metadata:
  name: vertsh
  namespace: vertsh
  labels:
    app.kubernetes.io/name: vertsh
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: vertsh-http
  selector:
    app.kubernetes.io/name: vertsh
