apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: sealed-secrets
  namespace: kube-system
spec:
  interval: 5m
  chart:
    spec:
      chart: sealed-secrets
      # https://bitnami.github.io/sealed-secrets/index.yaml
      version: '2.19.1'
      sourceRef:
        kind: HelmRepository
        name: sealed-secrets
        namespace: kube-system
      interval: 5m
  # https://github.com/bitnami/sealed-secrets/tree/main/helm/sealed-secrets
  # https://github.com/bitnami/sealed-secrets/blob/main/helm/sealed-secrets/values.yaml
