apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: birdnet-go-cert
  namespace: birdnet-go
spec:
  secretName: birdnet-go-certificate-secret
  dnsNames:
    - "birdnet-go.homelab.rivetcode.com"
  issuerRef:
    name: cloudflare-clusterissuer
    kind: ClusterIssuer
