apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: openwebui-cert
  namespace: openwebui
spec:
  secretName: openwebui-certificate-secret
  dnsNames:
    - "openwebui.homelab.rivetcode.com"
  issuerRef:
    name: cloudflare-clusterissuer
    kind: ClusterIssuer
