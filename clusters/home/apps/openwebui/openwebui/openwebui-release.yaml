apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: openwebui
  namespace: openwebui
spec:
  interval: 5m
  chart:
    spec:
      chart: open-webui
      version: '15.1.0'
      sourceRef:
        kind: HelmRepository
        name: openwebui
        namespace: flux-system
      interval: 5m
  # https://github.com/open-webui/helm-charts/blob/main/charts/open-webui/values.yaml
  values:
    ollama:
      enabled: false
    image:
      repository: harbor.homelab.rivetcode.com/ghcr-proxy/open-webui/open-webui
    ollamaUrls:
      - "http://ollama-api.ollama.svc.cluster.local:11434"
