apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: config-paperlessngx
  namespace: kube-system
spec:
  interval: 10m
  dependsOn:
  - name: paperlessngx
  path: ./configs/
  prune: true # remove any elements later removed from the above path
  timeout: 2m
  sourceRef:
    kind: GitRepository
    name: paperlessngx-configs
  retryInterval: 1m
