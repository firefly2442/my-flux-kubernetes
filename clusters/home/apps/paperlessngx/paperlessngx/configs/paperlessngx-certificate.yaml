apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: paperlessngx-cert
  namespace: paperlessngx
spec:
  secretName: paperlessngx-certificate-secret
  dnsNames:
    - "paperlessngx.homelab.rivetcode.com"
  issuerRef:
    name: cloudflare-clusterissuer
    kind: ClusterIssuer
