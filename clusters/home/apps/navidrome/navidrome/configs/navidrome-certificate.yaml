apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: navidrome-cert
  namespace: navidrome
spec:
  secretName: navidrome-certificate-secret
  dnsNames:
    - "navidrome.homelab.rivetcode.com"
  issuerRef:
    name: cloudflare-clusterissuer
    kind: ClusterIssuer
