apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mlflow-cert
  namespace: mlflow
spec:
  secretName: mlflow-certificate-secret
  dnsNames:
    - "mlflow.homelab.rivetcode.com"
  issuerRef:
    name: cloudflare-clusterissuer
    kind: ClusterIssuer
