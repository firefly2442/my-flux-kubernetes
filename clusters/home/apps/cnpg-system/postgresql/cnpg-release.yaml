apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cnpg
  namespace: cnpg-system
spec:
  interval: 5m
  chart:
    spec:
      # https://cloudnative-pg.io/charts/index.yaml
      chart: cloudnative-pg
      version: '0.29.0'
      sourceRef:
        kind: HelmRepository
        name: cnpg
        namespace: flux-system
      interval: 5m
