apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: searxng-cert
  namespace: searxng
spec:
  secretName: searxng-certificate-secret
  dnsNames:
    - "searxng.homelab.rivetcode.com"
  issuerRef:
    name: cloudflare-clusterissuer
    kind: ClusterIssuer
