apiVersion: v1
kind: Service
metadata:
  name: volcano-dashboard
  namespace: volcano-system
spec:
  selector:
    app: volcano-dashboard
  ports:
    - name: frontend
      port: 80
      targetPort: 8080
      protocol: TCP
