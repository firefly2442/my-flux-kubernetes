apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: authentik-setup

namespace: authentik

resources:
  - authentik-setup-job.yaml

configMapGenerator:
  - name: authentik-setup-script
    files:
      - setup-authentik.py=setup_authentik.py
