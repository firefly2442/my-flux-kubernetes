apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: memos-cert
  namespace: memos
spec:
  secretName: memos-certificate-secret
  dnsNames:
    - "memos.homelab.rivetcode.com"
  issuerRef:
    name: cloudflare-clusterissuer
    kind: ClusterIssuer
