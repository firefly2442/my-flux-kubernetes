apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: jellyfin-cert
  namespace: jellyfin
spec:
  secretName: jellyfin-certificate-secret
  dnsNames:
    - "jellyfin.homelab.rivetcode.com"
  issuerRef:
    name: cloudflare-clusterissuer
    kind: ClusterIssuer
